name: Validate marketplace

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check marketplace.json and README are in sync
        run: python3 scripts/generate-readme.py --check
